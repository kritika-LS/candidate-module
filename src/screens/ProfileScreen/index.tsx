import React from "react";
import { SafeAreaView } from "react-native";
import { TextStyle } from "../../components/common/Text";

export const ProfileScreen = () => {
    return(
        <SafeAreaView>
            <TextStyle>ProfileScreen</TextStyle>
        </SafeAreaView>
    )
}