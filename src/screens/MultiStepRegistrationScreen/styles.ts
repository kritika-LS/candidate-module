import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    scene: {
      flex: 1,
      padding: 16
    },
    input: {
      borderWidth: 1,
      borderColor: '#ccc',
      borderRadius: 6,
      padding: 10,
      marginBottom: 12
    }
});