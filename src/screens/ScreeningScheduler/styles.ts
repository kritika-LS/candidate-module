import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({

  safeArea: {
    flex: 1,
  },
  body: {
    flex: 1,
    padding: 16
  },
  screeningSchedularCard: {
    padding: 16,
    backgroundColor: '#fff',
    borderRadius: 8,
    marginTop: 12,
  }

})